// Tries printing a floating number

#include <stdio.h>
#include <stdlib.h>

int main(){
  const int num = 5; // A good practice is to have the variable name in uppercase
  printf("%d\n", num);
  return 0;
}